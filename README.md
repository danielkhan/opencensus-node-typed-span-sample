# OpenCensus Typed span sample

This is a sample project that shows how typed spans could be implemented in
opencensus-node.

The sample contains a POC for HTTP client spans.

This is discussed on a [dedicated issue](https://github.com/census-instrumentation/opencensus-specs/issues/257).